
= semantic_form_for [:admin, page], builder: ActiveAdmin::FormBuilder do |f|
	= f.inputs :name => "Page" do
		= f.input :title

	- f.has_many :components, allow_destory: true , new_record: true do | comp |
		= comp.inputs :name => "Component" do
			- comp.input :name
			-# - f.has_many :attachments, allow_destory: true , new_record: true do |attach|
			-# 	- if attach.object.asset.url.present?
			-# 		= image_tag(attach.object.asset.url, width: "200px")
			-# 	- attach.input :asset
	= f.actions