/ Section: Design Block
%section
  / Jumbotron
  .px-4.py-5.px-md-5.text-center.text-lg-start{:style => "background-color: hsl(0, 0%, 96%)"}
    .container
      .row
        .text-right
          =link_to root_path do
            %h5 Go to Home
      .row.gx-lg-5.align-items-center
        .col-lg-6.mb-5.mb-lg-0
          %h1.my-5.display-3.fw-bold.ls-tight
            The best offer
            %br/
            %span.text-primary for your business
          %p{:style => "color: hsl(217, 10%, 50.8%)"}
            Jadikan bisnismu lebih berkembang, dijelajahi oleh semua orang di seluruh dunia.
        .col-lg-6.mb-5.mb-lg-0
          .card
            .card-body.py-5.px-md-5
              .row
                %h1.text-center.mb-5 Login
              = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|

                / Email input
                .form-outline.mb-4
                  %label.form-label{:for => "form3Example3"} Email address
                  = f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control"
                / Password input
                .form-outline.mb-4
                  = f.password_field :password, autocomplete: "current-password", class: "form-control"
                  
                - if devise_mapping.rememberable?
                  .field.mb-4.text-right
                    = f.check_box :remember_me
                    = f.label :remember_me
                
                .actions
                  = f.submit "Login", class: "btn btn-primary btn-bloack mb-4 w-100"
                .text-center
                  = render "devise/shared/links"

