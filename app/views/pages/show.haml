

-# .container
-#   - @page.components.each do |component|
-#     =render partial: "components/#{component.layout.to_s}/#{component.layout.present? ? component.layout.to_s : "default"}", locals: { component:component }
-#     -# =render partial: "components/#{component.layout.to_s}/#{component.layout.to_s}", locals: {component: component} if component.layout.present?

- if ( components = @page.components ).present?
  - components.order(:position).each do |component|
    - case component.layout
    - when "banner", "list_images"
      .component{name: "#{component.slug}"}
        =render partial: "components/#{component.layout.to_s}/#{component.layout.to_s}", locals: {component: component} if component.layout.present?
    - else
      .container
        .component{name: "#{component.slug}"}
          =render partial: "components/#{component.layout.to_s}/#{component.layout.to_s}", locals: {component: component} if component.layout.present?

:javascript
  //$(".banner").parent().siblings("nav").addClass("position-absolute top-0")

  //$(".banner").parent().siblings("nav").css("z-index", "999")
  //$(".banner").parent().siblings("nav").css("background", "transparant")

  var owl = $('.owl-carousel');
  owl.owlCarousel({
    items: 1,
    loop: true,
    nav: false,
    autoplay:true,
    touchDrag: false,
    autoplayHoverPause:true,
    autoplayTimeout: 3000,
    smartSpeed: 1000,
    navText: [
      "<i class='fa fa-chevron-left'></i>",
      "<i class='fa fa-chevron-right'></i>"
    ],
  });

  $('.next-btn').click(function() {
      owl.trigger('next.owl.carousel');
  })

  $('.prev-btn').click(function() {
      owl.trigger('prev.owl.carousel');
  })





  $('.single-item-home').not('.slick-initialized').slick({
    autoplay:true,
    dots: true,
    infinite: true
  });

  $('.product-slick').not('.slick-initialized').slick({
    autoplay:true,
    dots: true,
    infinite: true
  });