- if (images = component.images).present?
  .row.text-center
    - images.each do |image|
      .col-6{class: "col-lg-#{12/images.size}"}
        =image_tag(image.asset.url(:perview), class: "w-50") 
        %br
        =image.title