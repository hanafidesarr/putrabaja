=render partial: "categories/show_layout/#{@category.layout.presence || 'default'}", locals: {comp: ""}

- if @page.components.present?
  - @page.components.order(:position).each do |component|
    %div{id: "loading_component-#{component.id}"}
    .component{name: "#{component.slug}"}
      =render partial: "components/#{component.layout.to_s}/#{component.layout.to_s}", locals: {component: component} if component.layout.present?
